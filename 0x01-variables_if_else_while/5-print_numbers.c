#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10
 * starting from 0, add a new line after code
 * Return: 0 is necessary
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
