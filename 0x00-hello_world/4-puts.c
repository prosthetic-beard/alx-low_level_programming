#include <stdio.h>

/**
 * main - prints "Programming is like building a multilingual puzzle
 * Return: 0 if program runs else, return something else
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
